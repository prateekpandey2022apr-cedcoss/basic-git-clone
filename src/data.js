const allUsers = ["Mojombo", "defunkt", "pjhyett", "wycats"];

export default allUsers;

// setDetails(response);
// const _users = response.filter((item) => item.login.startsWith(value));
// _users.forEach((_user) => {
//   fetch(`https://api.github.com/users/${_user}`)
//     .then((response) => response.json())
//     .then((response) => response.json());
// });
